# Инструкция по деплою на GitHub Pages

Этот проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

## 📋 Предварительные требования

- Аккаунт GitHub
- Git установлен локально
- Node.js 20+ и npm установлены

## 🚀 Быстрый старт

### Шаг 1: Проверка сборки проекта

Убедитесь, что проект собирается без ошибок:

```bash
npm install
npm run build
```

Если сборка прошла успешно, вы увидите папку `dist/` с готовыми файлами.

### Шаг 2: Настройка GitHub Pages в репозитории

1. Перейдите в ваш репозиторий на GitHub: `https://github.com/01452/atelier-elegance`
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
4. Сохраните изменения

### Шаг 3: Деплой

Деплой происходит автоматически при каждом push в ветку `main`:

```bash
git add .
git commit -m "Configure deployment"
git push origin main
```

После push:
1. Перейдите в раздел **Actions** вашего репозитория
2. Вы увидите запущенный workflow **Deploy to GitHub Pages**
3. Дождитесь завершения (обычно 2-3 минуты)
4. После успешного деплоя сайт будет доступен по адресу:
   ```
   https://01452.github.io/atelier-elegance/
   ```

### Ручной запуск деплоя

Вы также можете запустить деплой вручную:
1. Перейдите в **Actions** → **Deploy to GitHub Pages**
2. Нажмите **Run workflow**
3. Выберите ветку `main` и нажмите **Run workflow**

## 🔧 Конфигурация

### Base Path

Проект настроен с base path `/atelier-elegance/` для GitHub Pages. Это настроено в `vite.config.ts`:

```typescript
base: process.env.NODE_ENV === "production" ? "/atelier-elegance/" : "/"
```

**Важно**: Если вы измените имя репозитория, обновите base path в `vite.config.ts` и `BrowserRouter` в `src/App.tsx`.

### React Router

React Router настроен для работы с GitHub Pages через `basename`:

```typescript
<BrowserRouter basename={import.meta.env.BASE_URL}>
```

### 404.html

Для правильной работы React Router на GitHub Pages автоматически создается файл `404.html` из `index.html` во время сборки. Это обрабатывается плагином Vite в `vite.config.ts`.

## 📝 Скрипты

- `npm run build` - Сборка проекта для production
- `npm run deploy` - Сборка проекта (деплой происходит через GitHub Actions)
- `npm run preview` - Предпросмотр production сборки локально

## 🐛 Troubleshooting

### Проблема: Белая страница после деплоя

**Причина**: Неправильный base path или проблемы с путями к ресурсам.

**Решение**:
1. Проверьте, что base path в `vite.config.ts` соответствует имени репозитория
2. Убедитесь, что `BrowserRouter` использует `basename={import.meta.env.BASE_URL}`
3. Проверьте консоль браузера на наличие ошибок загрузки ресурсов

### Проблема: 404 при обновлении страницы или прямом переходе на маршрут

**Причина**: GitHub Pages не знает о маршрутах React Router.

**Решение**:
1. Убедитесь, что файл `404.html` создается при сборке (проверьте папку `dist/`)
2. Проверьте, что плагин `copy404Plugin` активен в `vite.config.ts` для production режима
3. Убедитесь, что `404.html` идентичен `index.html` в папке `dist/`

### Проблема: Ресурсы (CSS, JS, изображения) не загружаются

**Причина**: Неправильные пути к ресурсам из-за base path.

**Решение**:
1. Проверьте, что все пути в коде используют относительные пути или `import.meta.env.BASE_URL`
2. Убедитесь, что base path в `vite.config.ts` заканчивается на `/`
3. Проверьте Network tab в DevTools браузера для точного определения проблемы

### Проблема: `GET https://01452.github.io/src/main.tsx net::ERR_ABORTED 404`

**Причина**: Браузер пытается загрузить исходный файл вместо собранного бандла. Возможные причины:
- Открыт неправильный URL (без `/atelier-elegance/`)
- Кэш браузера показывает старую версию
- GitHub Pages еще не обновился после деплоя

**Решение**:
1. **Убедитесь, что вы открываете правильный URL**:
   ```
   ✅ Правильно: https://01452.github.io/atelier-elegance/
   ❌ Неправильно: https://01452.github.io/
   ```

2. **Очистите кэш браузера**:
   - Chrome/Edge: `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
   - Или откройте сайт в режиме инкогнито/приватном режиме

3. **Проверьте, что деплой завершился**:
   - Перейдите в раздел **Actions** вашего репозитория
   - Убедитесь, что последний workflow завершился успешно (зеленая галочка)
   - Если workflow еще выполняется, дождитесь завершения

4. **Проверьте собранные файлы**:
   - В собранном `dist/index.html` должны быть пути вида `/atelier-elegance/assets/...`
   - Если видите `/src/main.tsx`, значит сборка прошла неправильно

5. **Принудительно обновите страницу**:
   - `Ctrl+F5` (Windows) или `Cmd+Shift+R` (Mac) для жесткой перезагрузки

### Проблема: GitHub Actions workflow не запускается

**Причина**: Неправильные настройки репозитория или workflow файла.

**Решение**:
1. Убедитесь, что файл `.github/workflows/deploy.yml` существует и правильно настроен
2. Проверьте, что в настройках репозитория включен GitHub Pages с источником **GitHub Actions**
3. Проверьте права доступа: в **Settings** → **Actions** → **General** → **Workflow permissions** должно быть **Read and write permissions**

### Проблема: Деплой завершается с ошибкой

**Причина**: Ошибки сборки или проблемы с правами доступа.

**Решение**:
1. Проверьте логи в разделе **Actions** для деталей ошибки
2. Убедитесь, что проект собирается локально: `npm run build`
3. Проверьте, что все зависимости установлены: `npm ci`
4. Убедитесь, что в настройках репозитория есть права на запись для GitHub Pages

## 🔍 Проверка деплоя

После успешного деплоя проверьте:

1. **Главная страница**: `https://01452.github.io/atelier-elegance/`
2. **Маршруты**: 
   - `/atelier-elegance/diensten`
   - `/atelier-elegance/galerij`
   - `/atelier-elegance/over-ons`
   - `/atelier-elegance/contact`
3. **Обновление страницы**: Обновите любую страницу - она должна работать без 404
4. **Консоль браузера**: Откройте DevTools и проверьте отсутствие ошибок

## 📚 Дополнительная информация

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [React Router Deployment](https://reactrouter.com/en/main/start/overview#deploying)

## 🔄 Обновление сайта

Для обновления сайта просто сделайте push изменений в ветку `main`:

```bash
git add .
git commit -m "Update content"
git push origin main
```

Деплой запустится автоматически через GitHub Actions.

