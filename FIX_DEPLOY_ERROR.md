# 🔧 Исправление ошибки деплоя

## Ошибка
```
GET https://01452.github.io/src/main.tsx net::ERR_ABORTED 404 (Not Found)
Uncaught TypeError: Cannot read properties of null (reading 'appendChild')
```

## ✅ Что было исправлено

1. **Исправлен путь в `index.html`**: используется абсолютный путь `/src/main.tsx` (Vite автоматически заменит его на правильный путь с base path при сборке)
2. **Исправлена проверка mode в `vite.config.ts`**: изменено с `process.env.NODE_ENV` на `mode`
3. **Обновлен GitHub Actions workflow**: добавлен явный `--mode production`

**Важно**: Vite автоматически заменяет `/src/main.tsx` в `index.html` на собранный бандл `/atelier-elegance/assets/index-XXX.js` при сборке. В собранном `dist/index.html` НЕ должно быть ссылки на `main.tsx`.

## 🚀 Что нужно сделать сейчас

### Шаг 1: Закоммитьте исправления

```bash
git add .
git commit -m "Fix deployment: correct base path and index.html script path"
git push origin main
```

### Шаг 2: Дождитесь завершения деплоя

1. Перейдите в раздел **Actions** вашего репозитория
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (2-3 минуты)
3. Убедитесь, что workflow завершился успешно (зеленая галочка)

### Шаг 3: Откройте правильный URL

**⚠️ ВАЖНО**: Убедитесь, что вы открываете правильный URL:

```
✅ Правильно: https://01452.github.io/atelier-elegance/
❌ Неправильно: https://01452.github.io/
```

### Шаг 4: Очистите кэш браузера

Если ошибка все еще появляется:

1. **Очистите кэш браузера**:
   - Chrome/Edge: `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
   - Выберите "Изображения и файлы в кэше" и очистите

2. **Или откройте в режиме инкогнито**:
   - `Ctrl+Shift+N` (Windows) или `Cmd+Shift+N` (Mac)

3. **Принудительно обновите страницу**:
   - `Ctrl+F5` (Windows) или `Cmd+Shift+R` (Mac)

## 🔍 Проверка

После деплоя проверьте:

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Убедитесь, что все запросы идут на `/atelier-elegance/...`
5. Проверьте, что нет ошибок 404

## 📝 Измененные файлы

- `index.html` - используется абсолютный путь `/src/main.tsx` (Vite заменит его при сборке)
- `vite.config.ts` - исправлена проверка mode
- `.github/workflows/deploy.yml` - добавлен явный mode production
- `DEPLOYMENT.md` - добавлен раздел troubleshooting для этой ошибки
- `DEBUG_DEPLOY.md` - создана подробная инструкция по диагностике проблемы

## ❓ Если проблема сохраняется

1. Проверьте, что деплой завершился успешно в разделе **Actions**
2. Убедитесь, что вы открываете правильный URL с `/atelier-elegance/`
3. Проверьте консоль браузера (F12) для деталей ошибки
4. Убедитесь, что в собранном `dist/index.html` пути начинаются с `/atelier-elegance/`

