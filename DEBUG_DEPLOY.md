# 🔍 Диагностика проблемы с деплоем

## Текущая ошибка
```
main.tsx:1 Failed to load resource: the server responded with a status of 404 ()
panel.js:39 Uncaught TypeError: Cannot read properties of null (reading 'appendChild')
```

## ✅ Проверка локальной сборки

Собранный файл `dist/index.html` **правильный** - там нет ссылки на `main.tsx`, только на собранный бандл:
```html
<script type="module" crossorigin src="/atelier-elegance/assets/index-XXX.js"></script>
```

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте, что деплой завершился

1. Перейдите в раздел **Actions** вашего репозитория на GitHub
2. Убедитесь, что последний workflow "Deploy to GitHub Pages" завершился успешно (зеленая галочка)
3. Если workflow еще выполняется или завершился с ошибкой, дождитесь успешного завершения

### Шаг 2: Проверьте правильный URL

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Убедитесь, что вы открываете правильный URL:

```
✅ ПРАВИЛЬНО: https://01452.github.io/atelier-elegance/
❌ НЕПРАВИЛЬНО: https://01452.github.io/
```

Если вы открываете сайт без `/atelier-elegance/` в конце, GitHub Pages будет искать файлы в корне, и вы получите ошибку 404.

### Шаг 3: Проверьте, что загружается правильный HTML

1. Откройте сайт: `https://01452.github.io/atelier-elegance/`
2. Нажмите `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows) для открытия DevTools
3. Перейдите на вкладку **Network**
4. Обновите страницу (`F5` или `Cmd+R`)
5. Найдите запрос к `index.html` или главной странице
6. Нажмите на него и перейдите на вкладку **Response** или **Preview**
7. Проверьте содержимое HTML - там **НЕ должно быть** строки `<script type="module" src="/src/main.tsx"></script>`
8. Вместо этого должна быть строка вида: `<script type="module" crossorigin src="/atelier-elegance/assets/index-XXX.js"></script>`

### Шаг 4: Очистите кэш браузера

Если в HTML все еще есть ссылка на `main.tsx`, значит браузер показывает старую версию:

**Chrome/Edge:**
1. Нажмите `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
2. Выберите "Изображения и файлы в кэше"
3. Выберите "За все время"
4. Нажмите "Удалить данные"

**Или используйте жесткую перезагрузку:**
- Windows: `Ctrl+F5` или `Ctrl+Shift+R`
- Mac: `Cmd+Shift+R`

**Или откройте в режиме инкогнито:**
- Windows: `Ctrl+Shift+N`
- Mac: `Cmd+Shift+N`

### Шаг 5: Проверьте кэш GitHub Pages

GitHub Pages может кэшировать старые версии файлов. Попробуйте:

1. Добавьте параметр к URL для обхода кэша:
   ```
   https://01452.github.io/atelier-elegance/?v=2
   ```

2. Или подождите 5-10 минут после деплоя - GitHub Pages может обновляться с задержкой

### Шаг 6: Проверьте консоль браузера

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Console**
3. Найдите все ошибки 404
4. Проверьте, какие именно файлы не загружаются
5. Убедитесь, что пути начинаются с `/atelier-elegance/`

## 🛠️ Решение проблемы

### Если в HTML все еще есть ссылка на `main.tsx`:

1. **Убедитесь, что изменения закоммичены и запушены:**
   ```bash
   git status
   git add .
   git commit -m "Fix index.html script path"
   git push origin main
   ```

2. **Дождитесь завершения деплоя** (проверьте Actions)

3. **Очистите кэш браузера** (см. Шаг 4)

4. **Проверьте снова**

### Если пути в HTML правильные, но все еще есть ошибки:

1. Проверьте, что все пути в Network tab начинаются с `/atelier-elegance/`
2. Убедитесь, что вы открываете правильный URL с `/atelier-elegance/` в конце
3. Проверьте, что файлы действительно существуют на GitHub Pages:
   - Откройте: `https://01452.github.io/atelier-elegance/assets/index-XXX.js`
   - Должен загрузиться JavaScript файл (не 404)

## 📋 Чеклист для проверки

- [ ] Деплой завершился успешно в разделе Actions
- [ ] Открываю правильный URL: `https://01452.github.io/atelier-elegance/`
- [ ] В HTML нет ссылки на `/src/main.tsx`
- [ ] В HTML есть ссылка на `/atelier-elegance/assets/index-XXX.js`
- [ ] Очистил кэш браузера
- [ ] Проверил в режиме инкогнито
- [ ] Все пути в Network tab начинаются с `/atelier-elegance/`

## 🆘 Если ничего не помогает

1. Проверьте логи в разделе **Actions** на GitHub - возможно, есть ошибки сборки
2. Убедитесь, что локально проект собирается: `npm run build`
3. Проверьте содержимое `dist/index.html` после локальной сборки
4. Убедитесь, что в настройках репозитория GitHub Pages использует **GitHub Actions** как источник

