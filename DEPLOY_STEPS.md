# 🚀 Шаги для деплоя на GitHub Pages

## ✅ Что уже сделано

1. ✅ Настроен `vite.config.ts` с правильным base path `/atelier-elegance/`
2. ✅ Обновлен `BrowserRouter` для работы с GitHub Pages
3. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
4. ✅ Добавлен плагин для автоматического создания `404.html`
5. ✅ Добавлен скрипт `deploy` в `package.json`
6. ✅ Проект успешно собирается локально

## 📝 Следующие шаги

### Шаг 1: Закоммитьте изменения

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push origin main
```

### Шаг 2: Включите GitHub Pages в настройках репозитория

1. Перейдите на GitHub: https://github.com/01452/atelier-elegance
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions` (не "Deploy from a branch")
4. Сохраните изменения

### Шаг 3: Проверьте деплой

1. После push в ветку `main` автоматически запустится workflow
2. Перейдите в раздел **Actions** вашего репозитория
3. Дождитесь завершения workflow "Deploy to GitHub Pages" (2-3 минуты)
4. После успешного деплоя сайт будет доступен по адресу:
   ```
   https://01452.github.io/atelier-elegance/
   ```

### Шаг 4: Проверьте работу сайта

- ✅ Главная страница загружается
- ✅ Навигация работает
- ✅ Обновление страницы не вызывает 404
- ✅ Все маршруты работают (`/diensten`, `/galerij`, `/over-ons`, `/contact`)

## 🔧 Измененные файлы

- `vite.config.ts` - добавлен base path и плагин для 404.html
- `src/App.tsx` - добавлен basename для BrowserRouter
- `package.json` - добавлен скрипт deploy
- `.github/workflows/deploy.yml` - создан workflow для автоматического деплоя
- `DEPLOYMENT.md` - подробная документация

## 📚 Подробная документация

См. файл `DEPLOYMENT.md` для:
- Детальных инструкций
- Troubleshooting
- Решения распространенных проблем

## ⚠️ Важно

Если вы измените имя репозитория, обновите base path в:
- `vite.config.ts` (строка 20)
- `src/App.tsx` (если нужно)

